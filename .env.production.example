# =============================================================================
# Project Orchestrator — Production Environment Variables
# =============================================================================
#
# Copy this file and set secure values:
#   cp .env.production.example .env
#   docker compose -f docker-compose.production.yml up -d
#
# WARNING: Change ALL default passwords before deploying!

# ---------------------------------------------------------------------------
# Neo4j
# ---------------------------------------------------------------------------

# Password for the Neo4j 'neo4j' user (REQUIRED — change from default!)
NEO4J_PASSWORD=change-me-please

# Neo4j ports (default: 7474 HTTP browser, 7687 Bolt protocol)
# NEO4J_HTTP_PORT=7474
# NEO4J_BOLT_PORT=7687

# Neo4j memory settings
# NEO4J_HEAP_INIT=512m
# NEO4J_HEAP_MAX=1G

# ---------------------------------------------------------------------------
# MeiliSearch
# ---------------------------------------------------------------------------

# Master key for MeiliSearch API (REQUIRED — change from default!)
MEILISEARCH_MASTER_KEY=change-me-please

# MeiliSearch port (default: 7700)
# MEILISEARCH_PORT=7700

# Max memory for indexing operations
# MEILISEARCH_MAX_MEMORY=512MiB

# ---------------------------------------------------------------------------
# Orchestrator
# ---------------------------------------------------------------------------

# Docker image tag (default: latest)
# Use a specific version for reproducible deployments: 1.0.0, 1.0, etc.
# ORCHESTRATOR_IMAGE_TAG=latest

# Server port (default: 8080)
# SERVER_PORT=8080

# Enable/disable frontend serving (default: true)
# Set to false for API-only mode
# SERVE_FRONTEND=true

# NATS message broker ports (default: 4222 client, 8222 monitoring)
# NATS_PORT=4222
# NATS_MONITOR_PORT=8222

# Log level (default: info,project_orchestrator=debug)
# RUST_LOG=info,project_orchestrator=debug

# ---------------------------------------------------------------------------
# Authentication (optional — uncomment to enable)
# ---------------------------------------------------------------------------
# By default, no authentication is required (open access).
# Uncomment and configure to enable auth.

# JWT signing secret (min 32 characters — generate with: openssl rand -base64 32)
# JWT_SECRET=

# Frontend URL for CORS and OAuth redirects
# AUTH_FRONTEND_URL=http://localhost:8080
